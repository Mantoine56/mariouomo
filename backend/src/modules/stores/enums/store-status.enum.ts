/**
 * Enum representing possible store statuses
 * Used to ensure type safety and consistency in store status values
 */
export enum StoreStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended'
}
